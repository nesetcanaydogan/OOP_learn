// in dev
public class NumberDigitCollector {
    public static void main(String[] args) {
        System.out.println("Type a number to collect it's digits (9 digit number max):");
        String userInput = System.console().readLine();
        int userNumber = Integer.parseInt(userInput);

        int digitsOfNumber = 1;
    }
}
